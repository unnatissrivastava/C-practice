#include <stdio.h>
#include<limits.h
int main(){
    int arr[] = [3,4,9,7,8,6];
    int max = INT_MIN;
    int sMax = INT_MIN;
    for (int i=0; i<6;i++{
        if(arr[i]>max){
            sMax = max;
            max = arr[i];
        }
        
        if(arr[i]<max && sMax<arr[i]){
            sMax = arr[i];
        }
    }
    printf("max element = %d/n",max);
    printf("second max element = %d",sMax);
    
    return 0;
}
